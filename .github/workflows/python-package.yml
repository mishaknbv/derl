# This workflow will install Python dependencies, run tests and lint with a
# variety of Python versions.
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-python-with-github-actions

name: Python package

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
        - os: macos-latest
          python-version: 3.13.13
        os: [ubuntu-latest]
        python-version: [3.13, 3.14]
      fail-fast: false

    steps:
    - uses: actions/checkout@v5
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v6
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install the package
      run: |
        python -m pip install --upgrade pip
        pip install -e '.[atari,mujoco]'
        pip install pylint
    - name: Lint with pylint
      continue-on-error: true
      run: |
        pylint derl
    - name: Run tests
      if: ${{ matrix.os == 'macos-latest' && matrix.python-version == '3.13.13' }}
      env:
        OMP_NUM_THREADS: "1"
        MKL_NUM_THREADS: "1"
        OPENBLAS_NUM_THREADS: "1"
      run: |
        pip freeze > default-requirements.txt
        pip uninstall -y -r default-requirements.txt
        pip install -r requirements.txt
        python -m unittest discover -s derl -p "*test.py"
